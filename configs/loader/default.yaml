# Shared by every stage; unknown keys forward verbatim to torch's DataLoader.
# `shuffle` is not here on purpose: training shuffles, evaluation does not.
batch_size: "${batch_size}"
num_workers: 0
pin_memory: false
drop_last: false
